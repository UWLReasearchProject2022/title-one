import { styled } from "@mui/system";
import { IconButton } from "@mui/material";

export const ToggleCollapse = styled(IconButton)({
  marginLeft: "auto",
  marginBottom: "1.2em",
  color: "#555555",
});
